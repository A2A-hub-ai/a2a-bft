# A2A-BFT dependencies
#
# 说明：核心协议库 src/a2a_bft/ 只使用 Python 标准库，本身不需要任何第三方包。
# 下列依赖仅用于「真实 LLM 推理」「部署 vLLM 服务」「数据集下载」与「绘图/统计」。
#
# 版本号对应论文 §6 中报告的实测环境（CUDA 12.8 / 2 × A800-SXM4-80GB）。
# 若只做协议逻辑验证（不需要 GPU），安装到 --- 推理 --- 之前的部分即可。

# --- 数据集下载 ---
datasets>=2.14.0

# --- 实验编排与统计 ---
numpy>=1.24.0
scipy>=1.10.0

# --- 绘图（论文矢量图 PDF 输出）---
matplotlib>=3.7.0

# ============================================================================
# 以下用于真实 LLM 推理与 vLLM 部署（需要 NVIDIA GPU）
# ============================================================================

# --- 推理客户端（OpenAI 兼容接口，用于调用 vLLM 与 DeepSeek API）---
# 注意：代码中是函数内惰性导入（deepseek_worker.py / multi_model_vllm.py），
#       因此缺少 openai 时仍可导入协议库、跑审计脚本与负向测试。
openai>=1.0.0

# --- GPU 推理栈（论文实测版本；请按 CUDA 版本选择对应 wheel）---
torch==2.8.0
transformers==4.56.2
vllm==0.11.0

# --- 模型权重下载（可选）---
huggingface_hub>=0.23.0

# --- 可选：LangGraph 集成（src/a2a_bft/legacy/ 中的实验性实现）---
# langgraph>=0.1.0
# langchain>=0.1.0
