# 2026-09-17 PAT 反馈修订 —— 补丁脚本存档

这些脚本是 **2026-09-17 依据 ICLR 2027 Paper Assistant Tool 反馈对
`papers/iclr2027_main.tex` 所做修改的完整记录**。

## 脚本清单（按执行顺序）

| 脚本 | 作用 | 修改条数 |
|------|------|---------|
| `_pat_patch.py` | 逐条落实 PAT 意见：声誉数字重测值、Definition 3.1、记号拆分、二项尾和、Algorithm 1 的 `ok` 谓词、Property 4/5/6、Lemma 5.3、附录 A.2/A.3.3/A.4.3/A.9/A.10、表 4 分母、延迟归因等 | 45 |
| `_pat_compress.py` | 正文压回 9 页（第 1 轮：措辞压缩） | 14 |
| `_pat_compress2.py` | 正文压回 9 页（第 2 轮：整行替换，带"关键数字必须保留"断言） | 9 |
| `_pat_compress3.py` | 正文压回 9 页（第 3 轮：§6.1 细节移入不计页数的 Reproducibility Statement） | 6 |
| `_pat_compress4.py` | 正文压回 9 页（第 4 轮：Limitations 改段首粗体、正文三表加 `\small`） | 8 |
| `_pat_compress5.py` | 正文压回 9 页（第 5 轮：最后一批压缩） | 5 |

每个脚本都内置**命中数断言**：任一条替换未按预期命中，则整个文件**不写入**
（`[ABORT] ... 文件未写入`）。因此这些脚本要么全部生效、要么完全不生效，不会留下半成品。
`_pat_compress2/3/4/5.py` 另外校验"新文本仍包含原有关键数字"，防止压缩时丢数据。

## ⚠️ 这些脚本已执行完毕，请勿重复运行

它们依赖**当时的行号与原始字符串**。`iclr2027_main.tex` 此后已经改变，
重新运行会因断言失败而安全中止（不会破坏文件），但也不会有任何效果。

## 结果

- 正文 **9 页**（结论完整落在第 9 页），总 27 页，编译 0 error / 0 warning / 0 undefined
- 分诊结论与证据见 [`../../audit/PAT_TRIAGE_2026-09-17.md`](../../audit/PAT_TRIAGE_2026-09-17.md)
